# This AWS SAM template has been generated from your function's configuration.
# If your function has one or more triggers, note that the AWS resources
# associated with these triggers aren't fully specified in this template and
# include placeholder values. Open this template in AWS Infrastructure Composer
# or your favorite IDE and modify it to specify a serverless application with
# other AWS resources.
AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: An AWS Serverless Application Model (SAM) template describing your function.
Resources:
  airflowschedulerETL:
    Type: AWS::Serverless::Function
    Properties:
      Architectures:
        - x86_64
      CodeUri: ./src
      Description: ''
      Environment:
        Variables:
          API_KEY: 0b3bdd4c0edb9ffd4b43e5b04ae8b3ed
          DB_USER: postgres
          DB_NAME: postgres
          DB_HOST: etl.c6zkcs228e5r.us-east-1.rds.amazonaws.com
          DB_PASSWORD: qwertyuiop
      EphemeralStorage:
        Size: 512
      Handler: lambda_function.lambda_handler
      LoggingConfig:
        LogFormat: Text
        LogGroup: /aws/lambda/airflow_scheduler_ETL
      MemorySize: 128
      PackageType: Zip
      RecursiveLoop: Terminate
      Runtime: python3.10
      RuntimeManagementConfig:
        UpdateRuntimeOn: Auto
      SnapStart:
        ApplyOn: None
      Tags: {}
      Timeout: 60
      Tracing: PassThrough
      Role: >-
        arn:aws:iam::549710416811:role/service-role/airflow_scheduler_ETL-role-huauwc0z
